<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="/css/master.css">
    <meta charset="utf-8">
    <title>Robots</title>
  </head>
  <body>

    <div class="people">
      {{#robots}}
      <div class="person">
        <a href="/users/{{id}}"><img class="images" src="{{avatar}}" alt=""></a>
        <a href="/users/{{id}}"><h2>{{name}}</h2></a>
        <div class="job-status">
          {{#job}}{{.}}, {{/job}}
          {{^job}}Available for hire{{/job}}
          {{company}}
        </div>
        </div>
        {{/robots}}
      </div>

  </body>
</html>
